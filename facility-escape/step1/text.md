# Room 1 - Reception Desk

The reception application is offline.

Employees report:

> "The application is healthy, but nobody can access it."

Investigate the Service.

Hints:

- kubectl get svc
- kubectl describe svc
- kubectl get endpoints

Fix the Service so traffic reaches the frontend pods.
